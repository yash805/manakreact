import React from 'react';

const Home = () => {
  // const navigate = useNavigate()

  return (
    <div>
      <h1>welcome to Home</h1>
      {/* <button onClick={()=> navigate('/filter')}>Filter page</button>
      <br />
      <button onClick={()=> navigate('/good')}>About page page</button> */}

    </div>
  )
}

export default Home
